# Core deep-RL stack
numpy
torch
gymnasium[atari,toy-text]>=0.29
ale-py>=0.8

# Taxi notebooks (tables / plots / hyperparameter search)
pandas
matplotlib
plotly
optuna

# Pong DQN notebook (Atari from pixels)
opencv-python
imageio[ffmpeg]

# Run the notebooks locally (already available on Google Colab)
jupyter
